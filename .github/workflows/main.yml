name: CI
on: [push]
jobs:
 build:
  runs-on: ubunto-latest
  steps:
   - uses: actions/checkout@v2
   - name: setup JDK 17
     uses: actions/setup-java@v2
     with:
      java-version: 17
      distribution: 'adopt'
   - name: Build with Gradle
     run: ./gradlew build
